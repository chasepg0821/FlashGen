import React from "react";

interface FCProps {
    prompt: string;
    answer: string;
    flipOnClick?: boolean;
}

const flipCard = () => {};

const FlashCard = ({ prompt, answer, flipOnClick }: FCProps) => {
    return <div>FlashCard</div>;
};

export default FlashCard;
